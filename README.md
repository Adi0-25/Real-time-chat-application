So basically its a basic project based on python , thread, numpy, pandas to build a real time chat bot. here we can perfrom real time chat with different users .There are several different features in it also like private message , group message, message for all etc.Run the server and client program both on different terminal using Visual Studio Code or any other python platfrom. Then give user name and run client program for each user simultaniously . Then enjoy the chat.
